# ╔═══════════════════════════════════════════════════════════════════════╗
# ║                    AcquireMock Configuration                         ║
# ╚═══════════════════════════════════════════════════════════════════════╝

# ===========================================================================
# 🗄️  DATABASE CONFIGURATION (REQUIRED)
# ===========================================================================

# Choose ONE of the following:

# Option 1: PostgreSQL (RECOMMENDED for production)
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/payment_db

# Option 2: SQLite (OK for development/testing)
# DATABASE_URL=sqlite+aiosqlite:///./payment.db

# Note: PostgreSQL offers better performance, concurrent connections, and reliability.
# For production use, always choose PostgreSQL.

# ===========================================================================
# 🔐 SECURITY CONFIGURATION (REQUIRED)
# ===========================================================================

# Webhook Secret Key
# This is used to sign webhooks with HMAC-SHA256
# MUST BE AT LEAST 32 CHARACTERS
# ⚠️  CHANGE THIS IN PRODUCTION!
WEBHOOK_SECRET=your_super_secret_string_min_32_chars_change_this_in_production

# Generate a secure secret:
#   Linux/macOS:  openssl rand -hex 32
#   Python:       python -c "import secrets; print(secrets.token_hex(32))"
#   Online:       https://www.random.org/strings/

# ===========================================================================
# 🌐 APPLICATION SETTINGS (REQUIRED)
# ===========================================================================

# Base URL of your application
# Used for generating payment links and redirects
BASE_URL=http://localhost:8000

# For production:
# BASE_URL=https://payments.yourcompany.com

# ===========================================================================
# 💰 CURRENCY SETTINGS (OPTIONAL)
# ===========================================================================

# Currency code (ISO 4217)
CURRENCY_CODE=USD

# Currency symbol displayed in UI
CURRENCY_SYMBOL=$

# Examples for other currencies:
# EUR: CURRENCY_CODE=EUR, CURRENCY_SYMBOL=€
# GBP: CURRENCY_CODE=GBP, CURRENCY_SYMBOL=£
# UAH: CURRENCY_CODE=UAH, CURRENCY_SYMBOL=₴
# JPY: CURRENCY_CODE=JPY, CURRENCY_SYMBOL=¥

# ===========================================================================
# 📧 EMAIL CONFIGURATION (OPTIONAL)
# ===========================================================================

# If NOT configured, OTP codes will be printed to console/logs
# This is fine for development but not for production!

# SMTP Server Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# ─────────────────────────────────────────────────────────────────────────
# Gmail Setup Instructions:
# ─────────────────────────────────────────────────────────────────────────
# 1. Enable 2-Factor Authentication: https://myaccount.google.com/security
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the 16-character password above (spaces removed)
#
# Example:
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USER=your.email@gmail.com
#   SMTP_PASS=abcd efgh ijkl mnop  (remove spaces: abcdefghijklmnop)

# ─────────────────────────────────────────────────────────────────────────
# Other Email Providers:
# ─────────────────────────────────────────────────────────────────────────
# Outlook:
#   SMTP_HOST=smtp-mail.outlook.com
#   SMTP_PORT=587
#
# SendGrid:
#   SMTP_HOST=smtp.sendgrid.net
#   SMTP_PORT=587
#   SMTP_USER=apikey
#   SMTP_PASS=your_sendgrid_api_key
#
# Mailgun:
#   SMTP_HOST=smtp.mailgun.org
#   SMTP_PORT=587
#   SMTP_USER=postmaster@your-domain.mailgun.org
#   SMTP_PASS=your_mailgun_password

# ===========================================================================
# 🐳 DOCKER-SPECIFIC SETTINGS (if using docker-compose)
# ===========================================================================

# These values are used by docker-compose.yml
# You can override them here or leave the defaults

# PostgreSQL Database Settings (for Docker)
POSTGRES_USER=acquiremock
POSTGRES_PASSWORD=acquiremock
POSTGRES_DB=acquiremock_db

# When using Docker, use this DATABASE_URL:
# DATABASE_URL=postgresql+asyncpg://acquiremock:acquiremock@db:5432/acquiremock_db

# ===========================================================================
# ⚙️  ADVANCED SETTINGS (OPTIONAL)
# ===========================================================================

# Application Port (default: 8000)
# PORT=8000

# Log Level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Rate Limiting
# MAX_REQUESTS_PER_MINUTE=10

# Payment Expiration Time (minutes)
# PAYMENT_EXPIRY_MINUTES=15

# Webhook Retry Configuration
# WEBHOOK_MAX_RETRIES=5
# WEBHOOK_TIMEOUT_SECONDS=10

# ===========================================================================
# 📝 NOTES
# ===========================================================================
#
# ✅ DO:
#   - Change WEBHOOK_SECRET to a strong random value
#   - Use PostgreSQL in production
#   - Keep this file secure (never commit to Git)
#   - Use environment-specific .env files (.env.production, .env.staging)
#
# ❌ DON'T:
#   - Commit this file to version control
#   - Share your WEBHOOK_SECRET
#   - Use default values in production
#   - Store real secrets here (use secrets manager instead)
#
# ===========================================================================
# 🆘 SUPPORT
# ===========================================================================
#
# Documentation: https://github.com/illusiOxd/acquiremock/tree/main/docs
# Issues: https://github.com/illusiOxd/acquiremock/issues
# Discussions: https://github.com/illusiOxd/acquiremock/discussions
#
# ===========================================================================